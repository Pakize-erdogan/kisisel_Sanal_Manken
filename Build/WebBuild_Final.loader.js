function UnityProgress(gameInstance, progress) {
    if (!gameInstance.Module)
        return;

    var container = document.getElementById("#unity-container");
    if (progress === 1) {
        // Yükleme bittiğinde hiçbir şey gösterme
    }
}